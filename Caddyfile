# Caddy is the only thing exposed to the internet. It terminates TLS and gets
# and renews certificates automatically. Auth is enforced by Flask itself
# (see web.py) via a login page — Caddy just proxies.

classes.bertbert.work {
	reverse_proxy web:8008

	encode gzip

	log {
		output stdout
		format console
	}
}
